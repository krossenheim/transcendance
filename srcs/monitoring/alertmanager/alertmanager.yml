global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'default'
    # Default receiver: a webhook placeholder. Replace with email/slack configs.
    webhook_configs:
      - url: 'http://localhost:5001/'
